# What is Python

Python is:
- interpreted (no compilation step like C)
- dynamically typed
- object-oriented

Everything in Python is an OBJECT.

Objects have methods accessed using dot notation:
example:
text = "hello"
text.upper()
